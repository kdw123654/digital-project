# v19 재설계: 단일 EPF의 학습 병목과 비교 조건 분리

이 문서는 시험 결과 확인 전에 `PLAN.json`에 동결한 실행 설계를 요약한다. 주제는 KNHANES 2021–2024 기반 만 19–39세의 비침습적 대사이상 조기 선별이며, 센서·설비·PPG·새 합성 과제로 확장하지 않는다. 기존 v17/v18 원본 코드·체크포인트·결과를 덮어쓰지 않는다.

## 고정하는 연구 계약

- 기존 미인지 대상자 4,068명, 동일한 19개 비침습 입력. 허리둘레·BMI·WHtR을 LR과 모든 신경망에 동일하게 제공한다.
- 정답은 혈당·혈압·TG·HDL의 네 동시 이상 소견이다. 혈액검사와 실제 혈압 수치를 입력에 넣지 않는다.
- 기존 5개 PSU 외부 fold, 각 fold의 fit/validation/calibration/threshold/test를 유지한다. 모든 역할에 2021–2024가 포함된다.
- 전처리, EPF의 초기 활성 조정과 상태 특징 정규화, 회귀 정답 변환은 clean fit 자료에만 맞춘다. 실제 원자료와 보존한 행 순서·값·정답·분할을 다시 대조한다.
- 주 분석 조건은 **basic_clean_native**로 고정한다. 다른 조건의 시험 최고값을 골라 주 결론으로 바꾸지 않는다.

## 모델 구조와 학습 병목

단일 상태 `(z, a, P)`의 복소 전파·quartic 위상·발화/리셋·반대칭 가소성 피드백을 유지한다. 세 전문가의 별도 출력 결합이나 원입력을 직접 처리하는 비선형 MLP 우회 경로를 만들지 않는다.

기존 구현의 진단에서 같은 입력 투영을 매 내부 단계에 반복하고, 마지막 readout의 0 초기화 때문에 첫 backward의 field gradient가 0이며, 일부 fit 입력에서 초기 발화율이 낮음을 확인했다. v19은 동일 입력/정적 계수 계산을 forward 내에 재사용하고, 불필요한 진단 집계는 요청할 때만 한다. 이 계산 변경은 원식과 forward/gradient 일치를 검증한다.

학습 초기화는 작은 비영 readout, clean fit 전체에 걸친 결정론적 표본으로 조정하는 초기 발화율, clean fit 상태 특징의 평균/척도를 사용한다. 목표 발화율 0.15와 특징 척도 하한 등은 이번 설계값이며 최적성 또는 임상 효과가 입증된 수치가 아니다. 개선 전후 첫 gradient, 짧은 fit-only 학습, 동일 장치 실행 비용을 따로 검증한다. fit loss 감소를 검증 성능으로 보고하지 않는다.

`EPF`의 native와 anchor는 동일 topology와 학습 가능한 선형 경로를 갖는다. native는 LR/Ridge 계수를 쓰지 않고 독립 초기화한다. 이번 anchor 조건은 분류 모델에만 적용하며 fit/validation에서 정한 LR 계수로 선형 경로를 시작한다. 회귀·공동 학습 확장은 native 주 조건에서만 수행한다. 선형 경로는 이후에도 학습 가능하다. **장 상태의 기여를 구분하기 위해 선형 경로가 아예 없는 `EPF_field_only`도 별도로 튜닝·비교한다.** MLP는 별도의 대조 모델이며 EPF 안에서 호출되지 않는다.

## 세 조건의 분리

| 요인 | 수준 1 | 수준 2 |
|---|---|---|
| 입력 표현 | basic: 기본 인코딩 56열 | engineered: hinge12·성별 상호작용4를 포함한 72열 |
| 학습 누락 증강 | 원래 입력만 | 같은 사람의 원래 입력 + 20% 인공 누락 입력 |
| 선형 경로 시작 | 독립 native 초기화 | 분류 LR linear_anchor 초기화 |

basic은 72열의 `[0:24, 40:72]`이며 앞 56열을 잘라 쓰지 않는다. 기본 인코딩에도 WHtR, 설문 재코딩, 결측 처리, 범주 인코딩이 포함된다. ‘변환 없는 원시 19열’이라고 부르지 않는다.

8개 조합을 EPF와 MLP에 적용한다. 모델 후보는 주 조건의 seed42에서 fold별로 고른 뒤, 같은 설정을 다른 seed와 나머지 7조건에 고정 적용한다. 따라서 나머지 7조건은 같은 설정에서의 조건 효과를 보는 비교이며 각 조건을 최대로 튜닝한 성능표가 아니다.

증강 여부와 관계없이 validation은 clean으로 고정한다. 같은 고유 사람 B명의 batch마다 clean은 B행, paired는 같은 B명의 원래/누락 2B행 손실을 합해 **한 번** 업데이트한다. paired의 각 행 가중치는 절반으로 하여 사람별 총 가중치와 epoch당 optimizer update 수를 맞춘다. LR의 clean/paired20 네 기준 조건에서도 paired 행의 가중치를 절반으로 하여 유효 가중치 합과 규제 강도가 두 배로 바뀌지 않게 한다. Ridge는 basic_clean 전용 수치 회귀 기준선이며 paired 조건은 실행하지 않는다. 증강의 추가 계산 비용은 숨기지 않는다.

## 후보 탐색과 목표

주 조건에서 EPF, EPF_field_only, MLP에 각각 8개 후보를 준다. 모델별 차원·깊이·갱신 횟수·학습률·규제 범위를 사용하며 파라미터 수를 억지로 동일하게 강제하지 않는다. 실제 파라미터·업데이트 수·시간·메모리를 함께 보고한다. 후보 목록은 실행 PLAN.json에 고정한다.

선택은 조사 가중 clean validation BCE 최솟값, 동률은 작은 파라미터 수와 후보 ID 순이다. 시험 자료는 후보 선택에 쓰지 않는다. 최소40/최대400 epoch, 정체32 epoch와 두 번 이상의 학습률 감소, gradient clipping5를 적용한다. 최적 epoch가 초기 상태인지, 예산 끝에서도 개선 중인지 구분한다. dropout을 포함한 재개의 재현성을 위해 NumPy·PyTorch CPU/CUDA RNG와 optimizer·scheduler·현재/최고 가중치를 함께 저장한다.

주 비교는 네 소견 분류다. 추가로 basic_clean_native의 EPF와 MLP에 대해 선택된 backbone·학습 설정을 고정하고 수치 전용과 공동 학습을 실행한다. 이때 출력 수는 각각 4/5/9로 바뀌므로 목적함수만의 순수 효과라고 부르지 않는다. field-only에는 이 추가 두 모드를 반복하지 않는다.

분류 손실은 fit-label의 가중 prevalence 상수 예측이 낸 BCE, 회귀 손실은 fit-only 표준화 정답에 대한 상수0 예측 MSE로 정규화한다. 공동 학습은 두 정규화 손실을 0.5/0.5로 평균한다. native의 초기화와 손실 기준에 LR/Ridge 계수나 성능을 사용하지 않는다.

## 확률 보정과 선별

모든 모델의 소견별 단조 보정은 같은 clean calibration 역할에서 맞춘다. 이 단계의 표본 수·유효 표본 수·각 소견 양성 수·계수 경계를 기록한다.

독립 합성 위험 `q0=1−product(1−p)`도 그대로 저장한다. 추가 보정은 `L=max(p)`, `U=min(sum(p),1)` 사이에서 하나의 γ를 calibration 자료만으로 학습한다.

- γ<0: `(1+γ)q0−γL`
- γ≥0: `(1−γ)q0+γU`

γ=0은 원래 계산이다. 항상 주변 확률과 합집합 범위가 일관되고 개별 p 증가에 따라 위험이 감소하지 않지만, 사람 간 q0 순위까지 보존하는 것은 아니다. 따라서 any AP 변화는 이 공통 보정의 영향과 모델 구조의 영향을 구분한다. 주변 확률 자체의 편향과 종합 위험의 편향도 분리한다.

raw/adjusted 위험 각각에 대해 별도 threshold 역할에서 민감도90/95 목표 컷오프를 정하고 test에 고정 적용한다. 시험 ROC에서 같은 민감도로 보간한 기술적 비교값은 운영 컷오프와 구분한다.

## 결과와 보존

5개 fold×3 seed의 OOF 성능을 평균하며 3 seed를 하나의 추론 앙상블로 합치지 않는다. 주 조건 EPF–MLP/LR/field-only 대조, 8조건 변화, 회귀/공동 확장은 구분한다. 2,000회 paired 전체 조사 PSU bootstrap은 고정 모델의 조건부 구간이며 전체 재학습 불확실성이 아니다. 이미 탐색한 코호트의 결과이므로 신규 외부 검증으로 부르지 않는다.

학습 의존 코드·입력·원자료·분할 해시를 고정하고, 변경된 자료로 기존 checkpoint를 조용히 재사용하지 않는다. 분석 코드 해시는 별도로 기록한다. 실행 산출물은 `artifacts/clinical_factorial_v19/`에, 개인별 값은 private 파일로만 보존한다. 공개 범위는 모델 코드·파라미터·집계·실행된 결과 노트북이다.
