# 통합 노트북 편집·실행 기록

`my_model_정리.ipynb`는 사용자 제공 기준선에서 후속 EPF 모델 설명과 v28 비교로 이어지는 통합본이다. 수정 전 다운로드 원본과 이전 공개본은 로컬 백업으로 보존했다. 앞부분 분석·학습·개인별 지표 계산은 재실행하지 않았고 기존 저장 결과를 유지했다. 2026-09-27 모델 설명 확장에서 새 집계 코드 7개만 원자료 없는 임시 작업 폴더에서 실행했다. 이어진 전체 편집은 설명과 배치를 정리했으며, 코드 21셀과 저장 출력 전체를 그대로 보존했다.

## 파일 무결성

- 제공 원본 SHA-256: `40f5753553b9ca8f8e94517bff13763dcc70f2cc77c4d7fe5f1d4af7b5eb3f6e`
- 확장 전 공개 노트북 SHA-256: `6a72bcbb4585cce367754a33c13307a13bd8e688ae7e3aab379a9222411de159`
- 현재 통합 노트북 SHA-256: `7573f2e65e77d77b593653554571059f320b028eddfa4ab5533eeff45c85ef48`
- 공개 파일은 UTF-8·LF로 저장했다. 위 공개 해시는 Git에 저장될 LF 바이트 기준이다.

## 최초 공개용 변경

셀 번호는 **0부터 시작**한다. 아래 최초 공개·모델 설명 확장 절의 셀 번호와 절 번호는 **각 편집 당시 버전**을 가리킨다. 현재 위치는 전체 편집 절과 노트북의 목차·코드 ID 장부를 따른다.

- **0**: 공개용 수정 및 미재실행 안내, 원문 추가 검토 수치의 출처, 초기 기준선과 v28 범위 차이를 명시했다.
- **3**: 원자료 파일 위치를 저장소의 `data/raw/`로 안내했다.
- **4, 6**: 컴퓨터에 고정된 경로를 `data/raw/hnYY_all.sas7bdat` 상대 위치로 바꾸었다. 저장된 개인별 5행 미리보기 출력(`text/html`, `text/plain`)을 제거하고, 해당 `head()` 코드를 크기·열 이름 출력으로 바꾸었다. 기존 집계 stdout은 유지했고 새 실행 결과를 만들지 않았다.
- **17**: 로컬 환경 경로가 포함된 stderr 출력만 제거했다. ROC 그림과 모델 성능표는 유지했다.
- **18**: 제거한 환경 출력에 관한 설명을 공개본 기준으로 갱신했다.
- 코드 또는 출력이 변경된 **4, 6, 17**의 `execution_count`는 비웠다. 나머지 셀의 코드·집계 출력·차트는 보존했다. 첨부 파일과 개인 정보를 담은 셀 메타데이터는 없었다.

## 2026-09-27 모델 설명 확장

- 총 70셀: 기존 41셀에 새 29셀을 추가했다. 0셀의 소개·목차와 40셀의 요약·다음 질문을 다듬고, 기존 1–39셀과 14개 분석 코드 셀의 코드·출력·메타데이터·ID는 그대로 보존했다.
- 16–23절에 두 실험의 코호트/입력/목표 차이, EPF 상태 및 실제 갱신식, 선형 앵커와 평균/공분산 경로, 네 소견·16상태 확률, 기존 모델·선행연구와의 관계, v26 후보, v28 성능·H1/H2·epoch·파라미터·한계를 연결했다.
- 5개 선행 1차자료를 원리별로 인용했다. 세계 최초나 정확한 0 기여, 모든 데이터·지표에서의 구조 무관성을 주장하지 않는다.
- 새 코드 셀 **53, 54, 56, 59, 62, 65, 69**는 확정 집계값을 내장한다. 작업공간 `.venv` 커널에서 이 7개만 실행했으며 오류 0건, 새 PNG 3개를 확인했다. 기존 학습 셀을 실행하거나 원자료·개인별 예측·모델 체크포인트를 읽지 않았다.
- 내부 집계값을 공개 v28 보고서와 대조했고, 후반부 문장·수식을 소스와 독립 검토했다. 노트북 형식·셀 ID·기존 결과 보존·개인 경로 제외를 확인했다. 새 그림의 축·범례도 시각 검수했다.
- `Run All`로 전체를 실행하면 앞부분 원자료 분석과 모델 학습도 실행된다. 당시 70셀 버전에서 집계 결과만 재생성하는 시작점은 **21절 집계 코드**였다. 현재 버전의 시작점은 **5.3절 집계 준비 바로 아래 코드**이다. 새 셀의 실행 번호는 별도 커널 실행에서 부여된 것이며 전체 노트북을 처음부터 다시 실행했다는 뜻이 아니다.

## 2026-09-27 전체 설명과 구성 정리

- 전체를 **6부, 79셀**로 재구성했다. 연구 질문과 용어 → 초기 데이터·모델 비교 → 변수·오즈비·하위집단 해석 → EPF 계산과 공동분포 → v26/v28 실험 결과 → 결론·문헌·재현 안내 순서다.
- 모든 설명 셀을 검토·정리하고, 어려운 개념은 직관·기호·수식 순으로 풀었다. 기존 학습 코드의 일부 과도한 주석 표현은 주변 본문에서 바로잡았다.
- **21개 코드 셀 전체의 객체와 순서가 편집 전과 정확히 동일함**을 비교했다. 소스, 저장 출력, 실행 번호, ID, 코드 메타데이터를 포함한다. 내장 PNG 9개를 보존했으며 저장된 오류 출력은 0건이다. 이번 단계에서 학습이나 집계 코드 재실행은 하지 않았다.
- LR 우세의 원인, 오즈비의 인과 해석, 1인 가구의 매개 원인, 파라미터 수를 통한 속도 추론을 구분했다. v28에서 AP와 NLL의 서로 다른 순위, H1/H2의 충족 범위, 표본·입력·정답이 다른 두 연구의 비교 한계를 명시했다.
- 노트북 형식, 고유 셀 ID, 목차 앵커, 저장소 상대 링크, 코드·출력 보존을 검증했다. 이전 실행·확장 메타데이터는 이력으로 보존하고 현재 셀 ID·위치 장부와 구분했다.
- 현재 집계 재생성은 **5.3절 바로 아래 코드부터 마지막 코드까지** 순서대로 실행한다. `Run All`은 초기 원자료 분석과 모델 학습도 포함한다.

## 분석 범위와 결과 해석

초기 기준선은 치료자 제외 후 5,204명에서 입력 결측을 제거한 **4,680명**, 입력 **11개**, **단일 이진 Target**(대사이상 소견 4개 중 1개 이상)을 사용한다. 70/30 분할의 학습 3,276명·시험 1,404명에서 원본 저장 결과의 로지스틱 ROC-AUC는 0.804777, 평균 정밀도(AP)는 0.695022다. 이것은 현재 v28의 결과가 아니다.

[v28 실험](../v28/REPORT.md)은 **4,068명**, 인코딩된 입력 **114차원**, 4개 위험 지표와 16개 조합 상태를 다루는 별도 5-fold 설계 비교다. 두 분석의 표본·입력·목표·평가 방식이 달라 성능 수치를 직접 비교할 수 없다.

원문의 검사값 결측 처리, 진단/치료 구분, 시험 데이터로 임계값 선택, 하위집단 학습 자료 재평가, 오즈비 추정에 관한 한계 설명을 유지했다. 원문에 `정리자 확인`으로 적힌 추가 수치와 SPSS 재현 주장은 이번 공개 준비에서 독립적으로 재검증하지 않았다. 임상 성능을 새로 검증한 공개본이 아니다.

## 재실행에 필요한 파일과 패키지

원자료는 저장소에 포함하지 않는다. `data/raw/`에 `hn21_all.sas7bdat`, `hn22_all.sas7bdat`, `hn23_all.sas7bdat`, `hn24_all.sas7bdat` 네 파일을 별도로 준비해야 한다. 노트북의 작업 디렉터리는 저장소 루트 또는 `notebooks/`를 사용한다. 이 노트북은 SAS 파일을 읽으며 v28의 SPSS 자료 경로와 별개다. 원래 수집 반복문은 없는 연도 파일을 건너뛰므로 네 파일이 모두 있어야 기록된 4개년 분석 범위를 유지한다.

초기 기준선 코드의 외부 패키지: `numpy`, `pandas`, `pyreadstat`, `scikit-learn`(`sklearn`), `scipy`, `matplotlib`, `seaborn`, `xgboost`, `lightgbm`. 노트북 실행에는 Jupyter 및 Python 커널도 필요하다. 표준 라이브러리는 `os`, `warnings`, `pathlib`이다. 원본 메타데이터의 Python 버전은 3.13.5이며, 원본 실행 패키지 버전 전체는 기록돼 있지 않다. 이 목록은 초기 기준선의 의존성 안내로, v28의 학습 의존성 목록과 구분한다.

통합 노트북의 앞부분을 실행하면 기준선 및 하위집단 모델을 새로 학습한다. 후반부는 내장한 확정 집계값으로 표·그림을 다시 만들며 v28의 1,000개 학습 단위를 수행하지 않는다. 별도 `v28/REPORT.ipynb`는 이와 달리 같은 폴더의 `PUBLIC_REPORT.json`을 읽는 원 집계 보고서다.
