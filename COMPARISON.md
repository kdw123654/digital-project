# 모델 비교와 추가 조정 결과

목적은 새로운 방법론을 주장하는 것이 아니라 **어떤 모델을 활용할지 고르는 것**이다.

## 현재 NN, 로지스틱, 트리의 차이

| 모델 | 계산 방식 | 네 소견 처리 | 주요 비용/장점 |
|---|---|---|---|
| LR_linear | 선형 log-odds | 네 독립 회귀 | 작고 단순 |
| LR_basis | hinge·성별×신체 파생 기저를 포함한 로지스틱 | 네 독립 회귀 | 강한 소형 기준선 |
| RF / HGB | 여러 조건 분기와 트리 결합 | 네 독립 모델 | 비선형 분할, 상대적으로 큰 저장/추론 비용 |
| 기존 NN | 신체·인구사회학·행태 표현과 곱셈 상호작용 | 종합 위험 + 조건부 15조합 | 공유 표현, 16조합 확률의 일관성 |
| 새 후보 | 위상 간섭 / 희소 갱신 / 결합 이완 | 네 소견 및 쌍별 점수 | 추가 구조의 실용적 이득을 실험 |

NN은 현재 765개 학습 파라미터, 배포용 LR_basis는 292개다. NN이 LR보다 작다는 주장은 하지 않는다.
16개 조합은 혈당·혈압·TG·HDL 이상 네 이진 라벨의 조합이며, 서로 배타적인 네 질환 분류가 아니다.

## 1차 비교

10개 계열 × 두 설정 × seed42/43/44 = 60회 적합. 같은 19개 비침습적 입력과 역할 분할을 사용했다.
검증 네 소견 macro AP 평균으로 선택하고, 보정·컷오프는 별도 2023년 자료로 정했다.
그룹 NN은 현재 구조를 같은 다중라벨 손실로 재학습한 `group_hurdle`이며 기존 저장 모델 자체와 구분한다.
NN은 joint NLL/4 + 0.2 marginal BCE, LR/트리는 각각의 표준 목적함수를 사용했다.

| 모델 | 2024 macro AP | macro Brier ↓ | 구현 p50 ms |
|---|---:|---:|---:|
| LR_linear | 0.3853 | 0.1025 | 0.2257 |
| LR_basis | 0.3972 | 0.1005 | 0.2178 |
| RandomForest | 0.4010 | 0.0992 | 6.8041 |
| HistGradientBoosting | 0.3623 | 0.1026 | 2.1917 |
| independent_mlp | 0.3968 | 0.1006 | 0.0881 |
| group_hurdle | 0.4026 | 0.1004 | 0.1730 |
| pair_energy | 0.3973 | 0.1006 | 0.1660 |
| phase_pair | 0.3914 | 0.1013 | 0.1610 |
| sparse_pair | 0.3969 | 0.1006 | 0.2834 |
| relax_pair | 0.3969 | 0.1006 | 0.1963 |

속도 표는 sklearn LR/트리, torch NN의 CPU 1스레드 실행이며 구조 자체의 속도 우위가 아니다.
별도 NumPy 경로에서는 LR p50 0.0243ms, 기존 NN p50 0.0272ms였다. 전처리·JSON·HTTP·모델 로딩 제외.
기존 저장 v5 NN의 별도 macro AP는 0.4038이며 학습/선정 목적이 달라 1차 재학습 비교와 혼동하면 안 된다.
검증에서 사전 선택된 계열은 phase_pair였지만 2024년에서는 최선이 아니었다.

## 왜 새 구조의 성능이 낮았는가?

확인한 사실: seed42 학습/검증 재실행에서 세 후보의 초기 AP는 약 0.397이었다.
40 epoch 후에는 phase 0.3858, sparse 0.3929, relax 0.3920으로 낮아졌고, 선형 가중치의 상대 변화량은 약43%였다.
여러 후보가 한 epoch를 최적으로 선택했다. 이는 강한 LR 초기값을 추가 학습이 훼손했을 가능성을 보여주지만 단일 원인의 인과 증명은 아니다.

입력은 정적 표형 자료다. 임의 잠재 채널의 위상 간섭이나 반복 계산을 추가한 것만으로 생물/물리 모델의 장점이 보장되지 않는다.
원래 모델에서 중요한 공간·시간 정보가 이 자료에 있다고 가정하지 않았다.

## 2차: 데이터와 사용 조건에 맞춘 수정

- **phase_reliable:** 신체·행태·인구사회학 그룹 단위의 진폭·위상 간섭. 관측된 입력 비율로 신호를 조절.
- **sparse_reliable:** 입력에서 상태를 시작하고, 희소 연결로 작은 누설 갱신을 수행. 사람마다 상태 초기화.
- **relax_label:** 임의 특징 채널 대신 네 소견의 확률 상태 사이에서 유계 관계 갱신.
- 세 후보 모두 train-only LR 기준을 고정하고 작은 잔차만 학습. 학습률도 0.001/0.0003으로 조정.

공정한 비교를 위해 **모든 LR·트리·NN에 같은 정상+누락 학습자료**를 제공했다.
나이·성별을 제외한 항목에20% 확률의 누락을 요청하고, 허리둘레/WHtR은 같이 제거했다.
기존 결측까지 포함한 학습 입력의 실제 결측률은20.82%였다. 검증/평가에는 같은 세 누락 마스크를 모델별로 공유했다.
정상과 누락 검증 AP를 반반 반영해 선택했다. 7개 계열 × 두 설정 × 세 seed =42회 적합.
2차는 AP·AUC·Brier 확률 비교이며 기존 컷오프 역할529명은 새 정책 선정에 사용하지 않았다.

| 모델 | 1차 정상 입력 AP | 2차 정상 입력 AP | 2차 누락 입력 AP |
|---|---:|---:|---:|
| LR_basis → LR_basis | 0.3972 | 0.4002 | 0.3714 |
| RandomForest → RandomForest | 0.4010 | 0.4091 | 0.3925 |
| HistGradientBoosting → HistGradientBoosting | 0.3623 | 0.3910 | 0.3777 |
| group_hurdle → group_hurdle | 0.4026 | 0.4080 | 0.3778 |
| phase_pair → phase_reliable | 0.3914 | 0.4015 | 0.3720 |
| sparse_pair → sparse_reliable | 0.3969 | 0.4027 | 0.3727 |
| relax_pair → relax_label | 0.3969 | 0.4025 | 0.3724 |

1차와2차 사이에는 구조뿐 아니라 학습 증강·학습률·보정 환경도 바뀌었다. 숫자 차이를 구조 하나의 효과로 해석할 수 없다.
같은 조건의 모델 간 비교는 각 단계 안에서 수행한다. 위 표의1차는 수정 전 참고값이다.

희소 후보의 최적 epoch는54/126/123이었다. 검증에서 사전 선택된2차 계열은 sparse_reliable이다.
같은2차 LR 대비 희소 후보의 AP 차이는 정상 입력 +0.0025, 누락 입력 +0.0013으로 작았다.
고정 모델 조건의 PSU 재표집95% 구간은 각각 [0.0004,0.0054], [0.0004,0.0028]이었다.
다중 비교 보정과 선택 불확실성을 포함하지 않고2024년을 재사용했으므로 확정적 우월성으로 주장하지 않는다.
랜덤포레스트는2차 누락 조건에서 수치상 가장 높았지만 LR 대비 차이 구간은0을 포함했다.

## 활용 결정

1. **현재 주 모델:** 기존 v5 NN 유지. 검증된 실행 경로를 교체할 명확한 이점이 없다.
2. **단순 기준/대체:** LR_basis. 작고 빠르며 성능 기준선으로 계속 유지.
3. **사용자 아이디어 중 후속 후보:** sparse_reliable. 수정 후 학습과 작은 개선을 확인했지만 기존 NN·RF를 능가하지는 못했다.
4. **입력 누락이 많은 환경의 비교 후보:** 증강 학습 RandomForest. 높은 평가 수치와 큰 추론/저장 비용을 함께 고려.
5. phase/relax는 자동 채택하지 않는다. 원래 아이디어가 일반적으로 실패했다는 의미도 아니다.

## 근거와 한계

- 2021–2022 학습1,680/검증372, 2023 보정481/컷오프529, 2024 평가1,006. 코호트는4,068명.
- 목표는 현재 검사 이상 소견이며 질환 진단/미래 발병이 아니다. `living_alone`은1인가구 대리변수다.
- 위상/희소/이완은 사용자 프로젝트 원리를 정적 표형 데이터에 맞춘 지도학습 구현이며, 실제 커넥톰·와류·물리 매질 재현은 아니다.
- 2차 누락은 인공 MCAR 스트레스다. 실제 누락 원인, 시간 정보, 자연 결측 분포에 대한 검증을 대신하지 않는다.
- 모든2024 결과는 탐색적이며 최적화 범위를 넓히면 순위가 바뀔 수 있다. 같은 설정 수가 같은 계산 예산을 뜻하지 않는다.
- 현재 저장 NN과 LR의 차이는 확실하지 않다. 좋은 결과가 나올 때까지 시험자료를 보고 재튜닝하지 않고 이번 두 단계를 종료했다.
- 전체 집계/조건은 `comparison.json`, 출고 모델의 확률 일치 검증은 `verification.json`에 있다.
